import 'package:flutter/material.dart';

class ListenBookPage extends StatelessWidget {
  const ListenBookPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}